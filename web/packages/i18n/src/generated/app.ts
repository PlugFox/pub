// AUTO-GENERATED by packages/i18n/scripts/i18n-gen.ts — DO NOT EDIT.
// Source: packages/i18n/messages/app.yaml. Regenerate: `bun run i18n:gen`.

export const app = {
  /** First focusable link in the app shell; jumps past the header to the main region. */
  skipToContent: { id: "app.skipToContent", en: "Skip to content" },
  /** Sidebar link to the app's start page (/app). */
  navOverview: { id: "app.navOverview", en: "Overview" },
  /** Sidebar link to the package list. */
  navPackages: { id: "app.navPackages", en: "Packages" },
  /** Sidebar link to the package search screen. */
  navSearch: { id: "app.navSearch", en: "Search" },
  /** Sidebar link to the list of organizations the user belongs to. */
  navOrgs: { id: "app.navOrgs", en: "Organizations" },
  /** Sidebar link to the CLI/API token list. */
  navTokens: { id: "app.navTokens", en: "Tokens" },
  /** Sidebar link to the list of signed-in devices. */
  navSessions: { id: "app.navSessions", en: "Sessions" },
  /** Sidebar link to the account screen (profile and security). */
  navAccount: { id: "app.navAccount", en: "Account" },
  /** Sidebar link to the notification center. */
  navNotifications: { id: "app.navNotifications", en: "Notifications" },
  /** Sidebar link to the instance administration panel. */
  navAdmin: { id: "app.navAdmin", en: "Admin" },
  /** Accessible name of the app's primary navigation landmark. */
  navPrimary: { id: "app.navPrimary", en: "Main navigation" },
  /** Placeholder and accessible label of the search field in the app header. */
  searchPlaceholder: { id: "app.searchPlaceholder", en: "Search packages" },
  /** Accessible name of the avatar button that opens the user menu in the header. */
  userMenu: { id: "app.userMenu", en: "Account menu" },
  /** Accessible name of the header control that switches the active organization. */
  orgSwitcher: { id: "app.orgSwitcher", en: "Organization" },
  /** Option in the organization switcher that clears the filter and shows everything. */
  orgSwitcherAll: { id: "app.orgSwitcherAll", en: "All organizations" },
  /** User-menu item that ends the current session and returns to the sign-in screen. */
  signOut: { id: "app.signOut", en: "Sign out" },
  /** Accessible name of the region where transient toast messages appear. */
  toasts: { id: "app.toasts", en: "Notifications" },
  /** Button that re-runs a failed request. */
  retry: { id: "app.retry", en: "Try again" },
  /** Button that closes a dialog without performing its action. */
  cancel: { id: "app.cancel", en: "Cancel" },
  /** Confirming button of a creation dialog (organization, token). */
  create: { id: "app.create", en: "Create" },
  /** Destructive button that invalidates a token or a session. */
  revoke: { id: "app.revoke", en: "Revoke" },
  /** Fallback error message when the server refused a request for an unrecognized reason. */
  genericError: { id: "app.genericError", en: "Something went wrong. Try again." },
  /** Error shown when the request never completed (offline, DNS, aborted) — not a refusal. */
  networkError: { id: "app.networkError", en: "Could not reach the server. Check your connection and try again." },
  /** Error for HTTP 429; `{seconds}` is the cool-down from the Retry-After header. */
  rateLimited: { id: "app.rateLimited", en: "Too many attempts. Try again in {seconds} s." },
  /** Toast shown when the server refused to refresh the session and the user was signed out. */
  sessionExpired: { id: "app.sessionExpired", en: "Your session has expired. Sign in again." },
  /** Heading of the 404 screen inside the app island (an unknown /app/* route). */
  notFoundTitle: { id: "app.notFoundTitle", en: "This page does not exist" },
  /** Body text of the app's 404 screen. */
  notFoundBody: { id: "app.notFoundBody", en: "The link may be outdated, or the page may have moved." },
  /** Link on the app's 404 screen that returns to /app. */
  backToOverview: { id: "app.backToOverview", en: "Back to overview" },
  /** Heading of the sign-in screen. */
  loginTitle: { id: "app.loginTitle", en: "Sign in" },
  /** Subtitle under the sign-in heading, explaining the email OTP flow. */
  loginSubtitle: { id: "app.loginSubtitle", en: "We will email you a one-time code." },
  /** Label of the email field on the first step of sign-in. */
  loginEmailLabel: { id: "app.loginEmailLabel", en: "Email" },
  /** Placeholder of the sign-in email field. Keep it a plausible address, not a translation. */
  loginEmailPlaceholder: { id: "app.loginEmailPlaceholder", en: "you@example.com" },
  /** Validation message when the sign-in form is submitted with an empty email field. */
  loginEmailRequired: { id: "app.loginEmailRequired", en: "Enter your email address." },
  /** Button that submits the email and requests a one-time code. */
  loginContinue: { id: "app.loginContinue", en: "Continue" },
  /** Heading of the second sign-in step, where the emailed code is typed. */
  loginCodeTitle: { id: "app.loginCodeTitle", en: "Enter your code" },
  /** Subtitle of the code step. `{email}` is the address the code was sent to. */
  loginCodeSubtitle: { id: "app.loginCodeSubtitle", en: "We sent an 8-digit code to {email}. It expires in 10 minutes." },
  /** Label of the 8-digit one-time code field. */
  loginCodeLabel: { id: "app.loginCodeLabel", en: "Sign-in code" },
  /** Button that submits the one-time code and completes the first factor. */
  loginVerify: { id: "app.loginVerify", en: "Sign in" },
  /** Button that requests a replacement one-time code. */
  loginResend: { id: "app.loginResend", en: "Send a new code" },
  /** Countdown next to the resend button while the cool-down is running. */
  loginResendIn: { id: "app.loginResendIn", en: "You can request a new code in {seconds} s" },
  /** Link on the code step that returns to the email step. */
  loginUseAnotherEmail: { id: "app.loginUseAnotherEmail", en: "Use a different email" },
  /** Uniform error for a rejected OTP, TOTP, or recovery code — deliberately says nothing about why (S-04). */
  loginInvalidCode: { id: "app.loginInvalidCode", en: "That code is not valid. Check it and request a new one if needed." },
  /** Word separating the email form from the single-sign-on provider buttons. */
  loginOidcDivider: { id: "app.loginOidcDivider", en: "or" },
  /** Label of a single-sign-on button; `{provider}` is the configured provider's display name. */
  loginOidcButton: { id: "app.loginOidcButton", en: "Continue with {provider}" },
  /** Heading of the sign-in step that asks for the second factor. */
  loginMfaTitle: { id: "app.loginMfaTitle", en: "Two-factor authentication" },
  /** Subtitle of the second-factor step when a TOTP code is expected. */
  loginMfaSubtitle: { id: "app.loginMfaSubtitle", en: "Enter the 6-digit code from your authenticator app." },
  /** Subtitle of the second-factor step when a recovery code is expected. */
  loginMfaRecoverySubtitle: { id: "app.loginMfaRecoverySubtitle", en: "Enter one of the recovery codes you saved when you turned on two-factor authentication." },
  /** Label of the 6-digit TOTP field. */
  loginMfaCodeLabel: { id: "app.loginMfaCodeLabel", en: "Authentication code" },
  /** Label of the recovery-code field. */
  loginMfaRecoveryLabel: { id: "app.loginMfaRecoveryLabel", en: "Recovery code" },
  /** Link that switches the second-factor step from the TOTP field to the recovery-code field. */
  loginMfaUseRecovery: { id: "app.loginMfaUseRecovery", en: "Use a recovery code instead" },
  /** Link that switches the second-factor step back from the recovery-code field to the TOTP field. */
  loginMfaUseApp: { id: "app.loginMfaUseApp", en: "Use your authenticator app instead" },
  /** Notice shown when the provider list could not be loaded; the email form still works. */
  loginProvidersUnavailable: { id: "app.loginProvidersUnavailable", en: "Single sign-on is temporarily unavailable." },
  /** Heading of the /auth/callback screen while the authorization code is being exchanged. */
  callbackTitle: { id: "app.callbackTitle", en: "Finishing sign-in…" },
  /** Heading shown when the OIDC callback was rejected or arrived without its flow handle. */
  callbackFailed: { id: "app.callbackFailed", en: "Sign-in could not be completed." },
  /** Explanation when the flow handle is missing from session storage (expired, or a different browser). */
  callbackMissingFlow: { id: "app.callbackMissingFlow", en: "This sign-in link is no longer valid. Start again from the sign-in page." },
  /** Link on the failed-callback screen that returns to /app/login. */
  callbackBackToLogin: { id: "app.callbackBackToLogin", en: "Back to sign-in" },
  /** Heading of the account screen. */
  accountTitle: { id: "app.accountTitle", en: "Account" },
  /** Tab of the account screen showing the user's profile summary. */
  accountTabProfile: { id: "app.accountTabProfile", en: "Profile" },
  /** Tab of the account screen holding two-factor authentication settings. */
  accountTabSecurity: { id: "app.accountTabSecurity", en: "Security" },
  /** Field label in the profile summary. */
  accountEmail: { id: "app.accountEmail", en: "Email" },
  /** Shown instead of the address when the account has no email (an anonymized or provider-only account). */
  accountEmailMissing: { id: "app.accountEmailMissing", en: "No email on file" },
  /** Field label in the profile summary. */
  accountDisplayName: { id: "app.accountDisplayName", en: "Display name" },
  /** Field label for the account creation date in the profile summary. */
  accountMemberSince: { id: "app.accountMemberSince", en: "Member since" },
  /** Badge next to a verified email address. */
  accountEmailVerified: { id: "app.accountEmailVerified", en: "Verified" },
  /** Badge next to an email address that has not been verified. */
  accountEmailUnverified: { id: "app.accountEmailUnverified", en: "Unverified" },
  /** Heading of the TOTP section on the account security tab. */
  securityTotpTitle: { id: "app.securityTotpTitle", en: "Two-factor authentication" },
  /** Explanation under the two-factor heading. */
  securityTotpBody: { id: "app.securityTotpBody", en: "Protect publishing and administrative actions with a code from an authenticator app." },
  /** Button that starts TOTP enrollment. */
  securityTotpEnable: { id: "app.securityTotpEnable", en: "Set up two-factor authentication" },
  /** Destructive button that removes the TOTP credential; requires a fresh second factor. */
  securityTotpDisable: { id: "app.securityTotpDisable", en: "Turn off two-factor authentication" },
  /** Instruction above the QR code during TOTP enrollment. */
  securityTotpScan: { id: "app.securityTotpScan", en: "Scan this code with your authenticator app." },
  /** Instruction above the base32 secret, the fallback for a camera-less setup. */
  securityTotpManual: { id: "app.securityTotpManual", en: "Cannot scan? Enter this secret manually." },
  /** Label of the field where the first live TOTP code is typed to complete enrollment. */
  securityTotpConfirmLabel: { id: "app.securityTotpConfirmLabel", en: "Code from the app" },
  /** Button that verifies the first TOTP code and stores the credential. */
  securityTotpConfirm: { id: "app.securityTotpConfirm", en: "Confirm and turn on" },
  /** Status line on the security tab when a TOTP credential exists. */
  securityTotpEnabled: { id: "app.securityTotpEnabled", en: "Two-factor authentication is on." },
  /** Heading of the panel that shows the recovery codes once, right after enrollment. */
  securityRecoveryTitle: { id: "app.securityRecoveryTitle", en: "Save your recovery codes" },
  /** Warning above the recovery-code list; the codes are never retrievable again (S-05). */
  securityRecoveryBody: { id: "app.securityRecoveryBody", en: "Each code works once and lets you sign in without your authenticator. This is the only time they are shown." },
  /** Button that saves the recovery codes as a text file. */
  securityRecoveryDownload: { id: "app.securityRecoveryDownload", en: "Download codes" },
  /** Button that dismisses the recovery-code panel. */
  securityRecoveryDone: { id: "app.securityRecoveryDone", en: "I have saved them" },
  /** Title of the confirmation dialog for removing the TOTP credential. */
  securityDisableTitle: { id: "app.securityDisableTitle", en: "Turn off two-factor authentication?" },
  /** Body of the confirmation dialog for removing the TOTP credential. */
  securityDisableBody: { id: "app.securityDisableBody", en: "Your account will be protected by the email code alone, and publishing tokens will need a fresh sign-in instead of a code." },
  /** Toast confirming that the TOTP credential was removed. */
  securityTotpDisabled: { id: "app.securityTotpDisabled", en: "Two-factor authentication is off." },
  /** Title of the dialog that asks for a fresh second factor before a sensitive action. */
  stepUpTitle: { id: "app.stepUpTitle", en: "Confirm it is you" },
  /** Body of the step-up dialog explaining why the code is being asked for. */
  stepUpBody: { id: "app.stepUpBody", en: "This action needs a fresh code from your authenticator app." },
  /** Button that submits the step-up code and re-runs the blocked action. */
  stepUpConfirm: { id: "app.stepUpConfirm", en: "Confirm" },
  /** Explanation shown in the step-up dialog for accounts without a second factor — their re-auth path is a fresh sign-in (S-06.a). */
  stepUpNoFactor: { id: "app.stepUpNoFactor", en: "This account has no authenticator app. Sign in again to continue." },
  /** Heading of the session list screen. */
  sessionsTitle: { id: "app.sessionsTitle", en: "Sessions" },
  /** Subtitle of the session list screen. */
  sessionsSubtitle: { id: "app.sessionsSubtitle", en: "Browsers and devices signed in to this account." },
  /** Accessible name of the sessions table and its scroll region. */
  sessionsTableLabel: { id: "app.sessionsTableLabel", en: "Active sessions" },
  /** Column header for the moment a session was created. */
  sessionsCreated: { id: "app.sessionsCreated", en: "Signed in" },
  /** Column header for a session's last recorded activity. */
  sessionsLastSeen: { id: "app.sessionsLastSeen", en: "Last seen" },
  /** Column header for the address the session signed in from. */
  sessionsIp: { id: "app.sessionsIp", en: "IP address" },
  /** Column header for the coarse user agent recorded at sign-in. */
  sessionsDevice: { id: "app.sessionsDevice", en: "Device" },
  /** Badge marking the row of the session making the request. */
  sessionsCurrent: { id: "app.sessionsCurrent", en: "This device" },
  /** Button that revokes every session except the current one; requires a fresh second factor. */
  sessionsRevokeAll: { id: "app.sessionsRevokeAll", en: "Sign out everywhere else" },
  /** Empty state of the session list when only the current session exists. */
  sessionsEmpty: { id: "app.sessionsEmpty", en: "No other sessions" },
  /** Toast confirming that one session was revoked. */
  sessionsRevoked: { id: "app.sessionsRevoked", en: "Session revoked." },
  /** Toast after revoking every other session; `{count}` is how many were revoked. */
  sessionsRevokedAll: { id: "app.sessionsRevokedAll", en: {"one":"{count} session revoked.","other":"{count} sessions revoked."} },
  /** Placeholder in a table cell when the server has no value for the field (IP, user agent). */
  unknownValue: { id: "app.unknownValue", en: "Unknown" },
  /** Heading of the CLI/API token screen. */
  tokensTitle: { id: "app.tokensTitle", en: "Tokens" },
  /** Subtitle of the token screen, stating the two-credential-plane rule. */
  tokensSubtitle: { id: "app.tokensSubtitle", en: "Credentials for the dart pub client and CI. They are separate from your browser session." },
  /** Accessible name of the token table and its scroll region. */
  tokensTableLabel: { id: "app.tokensTableLabel", en: "API tokens" },
  /** Button that opens the token creation dialog. */
  tokensNew: { id: "app.tokensNew", en: "New token" },
  /** Column header for the token's label and first-eight-character display hint. */
  tokensHint: { id: "app.tokensHint", en: "Token" },
  /** Column header listing a token's granted scopes. */
  tokensScopes: { id: "app.tokensScopes", en: "Scopes" },
  /** Column header for the organization a token is bound to. */
  tokensOrg: { id: "app.tokensOrg", en: "Organization" },
  /** Column header for a token's expiry date. */
  tokensExpires: { id: "app.tokensExpires", en: "Expires" },
  /** Column header for a token's last recorded use. */
  tokensLastUsed: { id: "app.tokensLastUsed", en: "Last used" },
  /** Cell value for a token that never expires or has never been used. */
  tokensNever: { id: "app.tokensNever", en: "Never" },
  /** Title of the token list's empty state. */
  tokensEmpty: { id: "app.tokensEmpty", en: "No tokens yet" },
  /** Body of the token list's empty state. */
  tokensEmptyBody: { id: "app.tokensEmptyBody", en: "Create a token to publish packages or to let CI read your private registry." },
  /** Title of the token creation dialog. */
  tokensCreateTitle: { id: "app.tokensCreateTitle", en: "New token" },
  /** Label of the free-text name field in the token creation dialog. */
  tokensLabelField: { id: "app.tokensLabelField", en: "Label" },
  /** Placeholder of the token label field; an example of a useful name. */
  tokensLabelPlaceholder: { id: "app.tokensLabelPlaceholder", en: "CI — release pipeline" },
  /** Group label of the scope checkboxes in the token creation dialog. */
  tokensScopesField: { id: "app.tokensScopesField", en: "Scopes" },
  /** Checkbox label for the read scope. The scope name itself stays in English (it is a wire value). */
  tokensScopeRead: { id: "app.tokensScopeRead", en: "read — resolve and download packages" },
  /** Checkbox label for the publish scope. The scope name itself stays in English (it is a wire value). */
  tokensScopePublish: { id: "app.tokensScopePublish", en: "publish — upload new versions" },
  /** Checkbox label for the retract scope. The scope name itself stays in English (it is a wire value). */
  tokensScopeRetract: { id: "app.tokensScopeRetract", en: "retract — retract and restore versions" },
  /** Checkbox label for the admin scope. The scope name itself stays in English (it is a wire value). */
  tokensScopeAdmin: { id: "app.tokensScopeAdmin", en: "admin — manage organization settings" },
  /** Label of the numeric lifetime field in the token creation dialog; the default is 90. */
  tokensExpiryField: { id: "app.tokensExpiryField", en: "Expires in (days)" },
  /** Notice in the token dialog when a step-up-gated scope is selected (S-06). */
  tokensStepUpNotice: { id: "app.tokensStepUpNotice", en: "Publishing and administrative scopes ask for a second-factor code before the token is issued." },
  /** Validation message when the token dialog is submitted without an organization. */
  tokensOrgRequired: { id: "app.tokensOrgRequired", en: "Choose an organization for this token." },
  /** Validation message when the token dialog is submitted with no scope selected. */
  tokensScopesRequired: { id: "app.tokensScopesRequired", en: "Choose at least one scope." },
  /** Heading of the panel that shows a freshly created token secret. */
  tokensSecretTitle: { id: "app.tokensSecretTitle", en: "Copy your token now" },
  /** Warning above a freshly created token secret (show-once, S-13). */
  tokensSecretBody: { id: "app.tokensSecretBody", en: "This is the only time the secret is shown. Store it somewhere safe." },
  /** Caption above the ready-to-paste `dart pub token add` command. */
  tokensSecretCli: { id: "app.tokensSecretCli", en: "Register it with the pub client:" },
  /** Title of the token revocation confirmation dialog. */
  tokensRevokeTitle: { id: "app.tokensRevokeTitle", en: "Revoke this token?" },
  /** Body of the token revocation dialog; `{name}` is the token's label. */
  tokensRevokeBody: { id: "app.tokensRevokeBody", en: "Anything using {name} stops working within a minute. This cannot be undone." },
  /** Toast confirming that a token was revoked. */
  tokensRevoked: { id: "app.tokensRevoked", en: "Token revoked." },
  /** Toast confirming that a token was created. */
  tokensCreated: { id: "app.tokensCreated", en: "Token created." },
  /** Heading of the organization list screen. */
  orgsTitle: { id: "app.orgsTitle", en: "Organizations" },
  /** Subtitle of the organization list screen. */
  orgsSubtitle: { id: "app.orgsSubtitle", en: "Each organization owns its packages, members, and registry URL." },
  /** Button that opens the organization creation dialog. */
  orgsNew: { id: "app.orgsNew", en: "New organization" },
  /** Label of the badge showing the caller's role in an organization. */
  orgsRole: { id: "app.orgsRole", en: "Your role" },
  /** Label of the per-organization virtual registry base shown on an organization card. */
  orgsRegistryUrl: { id: "app.orgsRegistryUrl", en: "Registry URL" },
  /** Title of the organization list's empty state. */
  orgsEmpty: { id: "app.orgsEmpty", en: "You are not in any organization yet" },
  /** Body of the organization list's empty state. */
  orgsEmptyBody: { id: "app.orgsEmptyBody", en: "Create one to publish private packages and invite your team." },
  /** Title of the organization creation dialog. */
  orgsCreateTitle: { id: "app.orgsCreateTitle", en: "New organization" },
  /** Label of the display-name field in the organization creation dialog. */
  orgsNameField: { id: "app.orgsNameField", en: "Name" },
  /** Label of the slug field in the organization creation dialog. */
  orgsSlugField: { id: "app.orgsSlugField", en: "URL slug" },
  /** Help text under the slug field; the slug is immutable (decision 19). */
  orgsSlugHint: { id: "app.orgsSlugHint", en: "Becomes the registry address and can never be changed." },
  /** Validation message when the organization dialog is submitted with an empty name. */
  orgsNameRequired: { id: "app.orgsNameRequired", en: "Enter a name." },
  /** Validation message when the organization slug is empty or malformed. */
  orgsSlugRequired: { id: "app.orgsSlugRequired", en: "Enter a slug of lowercase letters, digits, and hyphens." },
  /** Toast confirming that an organization was created. */
  orgsCreated: { id: "app.orgsCreated", en: "Organization created." },
  /** Heading of the single-organization screen. */
  orgDetailTitle: { id: "app.orgDetailTitle", en: "Organization" },
  /** Placeholder text on the organization detail screen while the management endpoints are being built. */
  orgDetailTodo: { id: "app.orgDetailTodo", en: "Members, invitations, packages, settings, and the audit log for this organization arrive with the management API." },
  /** Badge marking a screen whose backend endpoints do not exist yet. */
  comingSoon: { id: "app.comingSoon", en: "Coming soon" },
  /** Body of the placeholder card on the packages screen. */
  placeholderPackages: { id: "app.placeholderPackages", en: "The package index, version history, and README rendering land with the registry read API." },
  /** Body of the placeholder card on the search screen. */
  placeholderSearch: { id: "app.placeholderSearch", en: "Full-text search with filter tags lands with the search API." },
  /** Shown on the search placeholder when the user actually typed a query; `{query}` is their input. */
  placeholderSearchQuery: { id: "app.placeholderSearchQuery", en: "You searched for “{query}”. Nothing is wired up yet — this screen is a placeholder." },
  /** Body of the placeholder card on the notifications screen. */
  placeholderNotifications: { id: "app.placeholderNotifications", en: "The notification center lands with the event stream." },
  /** Body of the placeholder card on the admin screen. */
  placeholderAdmin: { id: "app.placeholderAdmin", en: "Instance settings, user and organization management, moderation, and the audit viewer land with the admin API." },
  /** Heading of the app's start screen. */
  overviewTitle: { id: "app.overviewTitle", en: "Overview" },
  /** Line on the overview screen naming the current user; `{name}` is their display name. */
  overviewSignedInAs: { id: "app.overviewSignedInAs", en: "Signed in as {name}" },
  /** Body of the placeholder card on the overview screen. */
  overviewTodo: { id: "app.overviewTodo", en: "Instance statistics, top packages, and recent activity land with the read API." },
} as const;
