[package]
name = "pub-kv"
description = "Kv (store + pub/sub broker) implementations: in-process (moka) and Redis."
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
async-trait = { workspace = true }
deadpool-redis = { workspace = true }
futures = { workspace = true }
moka = { workspace = true }
pub-config = { workspace = true }
pub-core = { workspace = true }
tokio = { workspace = true }
tokio-stream = { workspace = true }

[dev-dependencies]
# The Kv contract suite (decision 35) runs the same properties against MemoryKv and RedisKv;
# the Redis leg is gated by PUB_TEST_REDIS_URL through the shared backend gate.
pub-test-support = { workspace = true }
tokio = { workspace = true }
