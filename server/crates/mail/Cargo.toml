[package]
name = "pub-mail"
description = "Outbound email: lettre transport + askama templates (text+HTML multipart)."
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
pub-core = { workspace = true }
