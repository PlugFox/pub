[package]
name = "pub-test-support"
description = "Test-only helpers shared across crates: the optional-backend gate (decision 35)."
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
